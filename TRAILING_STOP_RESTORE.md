# تعليمات استعادة خاصية Trailing Stop

**تاريخ إيقاف الخاصية:** 2 أغسطس 2026

---

## الحالة الحالية

تم إيقاف خاصية **Trailing Stop** في النظام. وقف الخسارة (SL) لجميع الصفقات (المفتوحة والجديدة) ثابت ولا يتحرك.

---

## كيفية استعادة Trailing Stop

عند طلب استعادة الخاصية، اتبع الخطوات التالية بالترتيب:

### الخطوة 1: تعديل `config/thresholds.py`

افتح ملف `config/thresholds.py` وابحث عن السطر 165:

```python
TRAILING_ENABLED = False
```

غيّره إلى:

```python
TRAILING_ENABLED = True
```

### الخطوة 2: إعادة تشغيل النظام

بعد حفظ التعديل، أعد تشغيل البوت:

```bash
python -m app.main
```

---

## ملاحظات مهمة

1. **الصفقات المفتوحة وقت الاستعادة:** ستبقى SL الخاص بها كما هو (القيمة المحفوظة) ولن يتم تحريكها بأثر رجعي. Trailing Stop سيبدأ بالتأثير فقط على الصفقات التي تُفتح **بعد** الاستعادة.

2. **القيم الحالية لعوامل Trailing Stop** (في نفس الملف):

   | العامل | القيمة الحالية |
   |--------|---------------|
   | `TRAILING_ENABLED` | `False` (تم إيقافه) |
   | `TRAILING_ACTIVATION_MULTIPLIER` | `1.5` (يبدأ بعد ربح 1.5x من المخاطرة) |
   | `TRAILING_ATR_DISTANCE` | `1.5` (مسافة SL من أعلى سعر) |
   | `TRAILING_MIN_DISTANCE_PCT` | `0.3` (أدنى مسافة % من السعر) |
   | `TRAILING_MAX_DISTANCE_PCT` | `5.0` (أقصى مسافة % من السعر) |

3. **الموقع الوحيد للتعديل:** الخاصية تتحكم بها قيمة واحدة فقط في `config/thresholds.py` ولا تؤثر على أي ملف آخر.

---

## بعد الاستعادة: احذف هذا الملف

بمجرد إعادة استعادة Trailing Stop بنجاح، **يجب حذف هذا الملف** من المستودع لأنه يكون قد أدى غرضه.
