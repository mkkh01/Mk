# تقرير مراجعة مستودع Mk

**المستودع:** `mkkh01/Mk`
**الفرع المُراجع:** `main`
**آخر commit عند المراجعة:** `1e59d6d`
**تاريخ المراجعة:** 14 أغسطس 2026
**نطاق المراجعة:** البنية، الإعدادات، الأمان، واجهات HTTP، طبقة التخزين، الاختبارات، والفحص الساكن.

## الخلاصة التنفيذية

المستودع قابل للفهم ويحتوي على تقسيم منطقي إلى محركات تحليل، محاكاة، تخزين، مراقبة، وواجهات تشغيل. كما أن الكود يمر بفحص بناء Python الأساسي (`compileall`) دون أخطاء نحوية. لكن توجد مشكلات عالية الخطورة يجب معالجتها قبل اعتباره آمنًا للنشر: **أسرار حقيقية ظهرت في تاريخ Git**، وواجهات Dashboard/Workflow تبدو مكشوفة دون مصادقة، إضافة إلى مشكلات تشغيلية واختبارية واضحة.

| الأولوية | المشكلة | الأثر المحتمل | الحالة |
|---|---|---|---|
| حرجة | أسرار Telegram وبيانات اتصال Postgres/Supabase في تاريخ Git | الاستيلاء على البوت أو قاعدة البيانات حتى بعد حذف الأسرار من HEAD | مؤكدة من سجل Git |
| عالية | واجهات البيانات التشغيلية بلا مصادقة ظاهرة | كشف قرارات وصفقات وأداء النظام لأي طرف يصل إلى الخدمة | مؤكدة من الكود |
| عالية | الخدمة تستمر بعد فشل تشغيل المحرك | نشر خدمة تبدو سليمة بينما التداول/المحرك متوقف أو حالته غير متسقة | مؤكدة من الكود |
| متوسطة | اختبار المجموعة الكاملة عالق، ووجود 77 مخالفة Ruff | احتمال عدم اكتشاف regressions وتراكم دين تقني | مؤكدة جزئيًا/قابلة لإعادة الإنتاج |
| متوسطة | مسارا `GET /` متطابقان | المسار الثاني غير قابل للوصول أو سلوكه يعتمد على ترتيب التسجيل | مؤكدة من الكود |
| متوسطة | `.gitignore` لا يحمي `config/settings.py` رغم توثيق README عكس ذلك | إعادة ارتكاب إعدادات حساسة بالخطأ | مؤكدة من الملفات |
| منخفضة | تطبيق migrations تلقائيًا أثناء startup مع بقاء HTTP فعالًا عند فشل startup | صعوبة اكتشاف الفشل واحتمال اختلاف حالة المخطط بين البيئات | تحتاج مراجعة تشغيلية |

## النتائج التفصيلية

### 1. أسرار مكشوفة في تاريخ Git — خطورة حرجة

الفحص الحالي لا يعرض قيمة سرية فعلية داخل `config/settings.py`؛ الملف يقرأ القيم من متغيرات البيئة. لكن سجل Git يحتوي على قيم Telegram token وDSN لقاعدة Postgres تتضمن اسم مستخدم وكلمة مرور. ظهرت هذه القيم في commits تاريخية، منها `d8495ab` و`a318642`، كما أظهر `git log --all -p -- config/settings.py` قيمًا على نمط:

```text
_DEFAULT_TELEGRAM_TOKEN = "..."
_DEFAULT_DATABASE_URL = "postgresql://...:...@...supabase.com:5432/postgres"
```

وجود السر في تاريخ Git يعني أن حذف السطر من النسخة الحالية لا يكفي؛ يستطيع أي clone سابق أو مستخدم يملك صلاحية قراءة المستودع الوصول إليه. يجب **إلغاء/تدوير Telegram token وكلمة مرور قاعدة البيانات فورًا**، ثم تنظيف التاريخ باستخدام أداة معتمدة مثل `git filter-repo` أو BFG، مع force-push منسق وإبلاغ كل من لديه clone.

يوجد أيضًا تناقض مباشر: `README.md` يقول إن `config/settings.py` يجب أن يكون gitignored، لكن `.gitignore` يحتوي فقط على `__pycache__/` و`data/replay/`، و`git ls-files` يثبت أن `config/settings.py` ملف متتبع. حتى لو لم يعد يحتوي على أسرار، فهذا يمنع الحماية الوقائية المقصودة.

### 2. غياب المصادقة عن واجهات Dashboard وWorkflow — خطورة عالية

في `app/workflow_endpoints.py:75-187` توجد مسارات مثل `/status/{symbol}` و`/decisions/{symbol}` و`/trades/{symbol}`، وتعتمد فقط على `Depends(get_supabase_client)`. لم يظهر في الملف أو بقية تعريفات الواجهات استخدام `HTTPBearer` أو `Authorization` أو dependency للتحقق من هوية المستدعي. وبالمثل، تسجل `app/main.py:1506-1508` واجهات Dashboard وملفات static أثناء startup دون طبقة مصادقة ظاهرة.

هذه المسارات تعيد معلومات حساسة تشغيليًا: قرارات النظام، الثقة، أسباب الرفض، الصفقات، الأسعار، وPnL. كما أن `render.yaml` ينشر الخدمة كـ web service، و`main.py` يربط Uvicorn على `0.0.0.0`. لذلك ينبغي افتراض أن الواجهات قابلة للوصول من الشبكة ما لم توجد حماية خارجية غير موجودة في هذا المستودع.

الإصلاح المقترح هو إضافة مصادقة صريحة لجميع مسارات البيانات، ويفضل جعل Dashboard خلف reverse proxy أو جلسة آمنة، واستخدام allowlist للرموز، وتحديد rate limits، وعدم الاعتماد على إخفاء المسار. يجب إضافة اختبارات تفشل عند طلب endpoint دون credentials.

### 3. استمرار خادم HTTP بعد فشل تشغيل التطبيق — خطورة عالية

في `app/main.py:1490-1507` يتم إنشاء `CTApplication` ثم استدعاء `start()`. عند وقوع استثناء، يسجل الكود رسالة تفيد بأن Dashboard سيبقى نشطًا رغم أن bot/engine قد يكونان offline، ثم لا يعيد رفع الاستثناء ولا ينهي العملية. بعد ذلك تُسجل endpoints وتُركب ملفات Dashboard.

هذا السلوك قد يجعل منصة النشر تعتبر الخدمة healthy لأن HTTP يستجيب، بينما مكوّن التداول لا يعمل. النتيجة هي false readiness، وتشخيص متأخر، واحتمال اتخاذ قرارات تشغيلية بناءً على لوحة غير متصلة بالمحرك. ينبغي جعل فشل المكونات الأساسية fatal في بيئة الإنتاج، أو جعل `/ready` يعيد HTTP 503 عند عدم الجاهزية، مع health check منفصل يوضح حالة كل dependency.

### 4. الاختبارات لا تنتهي ضمن مهلة عملية — خطورة متوسطة إلى عالية

تشغيل `python3 -m pytest -q` علق وتجاوز مهلة 180 ثانية، ثم علق تشغيل مجموعة أصغر من الاختبارات ضمن مهلة محددة أيضًا. لم يمكن الحصول على summary نجاح/فشل مكتمل قبل الإيقاف. هذا ليس دليلًا وحده على bug في منطق الإنتاج، لكنه دليل مؤكد على أن test suite الحالية غير قابلة للتشغيل بشكل موثوق في بيئة نظيفة دون تشخيص إضافي.

يوجد 36 ملف اختبار تقريبًا، بينها اختبارات تكامل، بينما لا يظهر في `pytest.ini` إعداد واضح لمهل زمنية أو فصل صارم بين unit وintegration. يجب تحديد الاختبار العالق باستخدام `pytest --collect-only`, و`pytest --setup-show`, وmarkers للتكامل، وإضافة timeout، ثم جعل الاختبار الوحدوي لا يحتاج Redis/Supabase/Telegram حقيقيًا.

### 5. وجود 77 مخالفة Ruff — خطورة متوسطة

`ruff check .` أعاد **77 خطأ**، معظمها `F401` لاستيرادات غير مستخدمة، مع ظهور `F841` لمتغيرات محلية غير مستخدمة في `tests/unit/test_structure.py`. هذا لا يثبت ثغرة تشغيلية بحد ذاته، لكنه يخفض جودة الإشارة في CI ويجعل الأخطاء الحقيقية أصعب في المراجعة. ينبغي إضافة Ruff إلى CI، اعتماد configuration موحدة، وإصلاح المخالفات بدل إخفائها على مستوى المشروع.

### 6. تعريف مسار الجذر مرتين — خطورة متوسطة

في `app/main.py:1517-1520` يُعرّف `GET /` لإرجاع `app/static/index.html`، ثم في `app/main.py:1532-1534` يُعرّف `GET /` مرة ثانية لإرجاع JSON ترحيبي. تعريف المسار نفسه مرتين يخلق route shadowing؛ غالبًا سيُستخدم التعريف الأول ويصبح fallback غير قابل للوصول، ما يضلل الصيانة والاختبارات. يجب الاحتفاظ بتعريف واحد، أو تحويل fallback إلى مسار مختلف مع اختبار السلوك المقصود.

### 7. فجوة بين التوثيق والحماية الوقائية للإعدادات — خطورة متوسطة

يوثق `README.md` نسخ `config/settings.example.py` إلى `config/settings.py` ثم يقول إن الملف الناتج gitignored. لكن `.gitignore` لا يتضمن `config/settings.py` ولا نمطًا عامًا مثل `.env*`. ينبغي إضافة الملف إلى ignore، وإضافة pre-commit secret scan وCI secret scan، مع إبقاء `settings.example.py` خاليًا من أي قيمة قابلة للاستخدام.

### 8. مخاطر تشغيلية حول migrations وreadiness — خطورة منخفضة إلى متوسطة

التوثيق يذكر أن startup يطبق migrations تلقائيًا، بينما `app/main.py` يسمح ببقاء الخادم حتى عند فشل بدء `CTApplication`. في بيئة متعددة النسخ قد يؤدي ذلك إلى تشغيل نسخة HTTP بمخطط قاعدة غير مكتمل أو إلى إخفاء فشل migration. الأفضل تنفيذ migrations كخطوة نشر منفصلة ومراقبة، أو جعلها idempotent ومحصورة في عملية واحدة مع قفل migration، ثم منع readiness حتى يكتمل التحقق من المخطط والاتصالات.

## نقاط إيجابية

يستخدم الوصول إلى Postgres معاملات parameters مثل `$1` في المواضع التي تمت مراجعتها، ما يقلل خطر SQL injection التقليدي. كما أن ملفات SQL تضع قيودًا مهمة مثل المفاتيح الأساسية، uniqueness، وقيود الاتجاه والحالة. ويحتوي المشروع على طبقات واضحة نسبيًا وفصل جيد بين contracts وengine وstorage، إضافة إلى وجود اختبارات وحدوية واسعة من حيث العدد.

## خطة الإصلاح ذات الأولوية

| الترتيب | الإجراء | معيار الإغلاق |
|---|---|---|
| 1 | تدوير كل Telegram/DB credentials التي ظهرت في Git history | لا تعمل الأسرار القديمة، ونتيجة secret scan للتاريخ نظيفة |
| 2 | تنظيف التاريخ وإضافة `config/settings.py` إلى `.gitignore` | لا توجد أسرار في أي commit reachable، وCI يمنع إعادة الإضافة |
| 3 | حماية جميع endpoints وDashboard | طلب بلا credential يعيد 401/403، واختبارات auth موجودة |
| 4 | إصلاح startup/readiness | فشل dependency يمنع readiness أو ينهي العملية في الإنتاج |
| 5 | عزل اختبارات التكامل وإضافة timeout | unit suite تنتهي بزمن معروف، والتكامل يعمل بعلامة صريحة |
| 6 | معالجة 77 مخالفة Ruff وإضافته إلى CI | `ruff check .` ينجح دون استثناءات غير مبررة |
| 7 | حذف route المكرر وإضافة اختبار HTTP للسلوك الجذري | تعريف واحد واضح لـ `GET /` |

## حدود المراجعة

لم تُجرَ اتصالات فعلية إلى Supabase أو Redis أو Telegram، ولم تُنفذ أوامر نشر أو تغييرات في المستودع. لذلك لا يمكن من هذه المراجعة وحدها تقييم صلاحيات البنية السحابية الفعلية أو سياسات الشبكة خارج الكود. كما لم أعدّل الملفات؛ التقرير مخصص لاتخاذ قرار الإصلاح أولًا.

## الملفات والأدلة الرئيسية

| الملف/الموضع | سبب الأهمية |
|---|---|
| `config/settings.py:30-65` | قراءة الأسرار من البيئة مع fallback قابل للتشغيل بقيم placeholder |
| `.gitignore` | لا يحمي `config/settings.py` |
| `app/workflow_endpoints.py:75-187` | endpoints بيانات دون dependency مصادقة ظاهرة |
| `app/main.py:1490-1508` | إبقاء HTTP فعالًا بعد فشل startup |
| `app/main.py:1517-1534` | تعريف `GET /` مرتين |
| `requirements.txt` | الاعتماديات المثبتة وإمكانات إعادة الإنتاج |
| `storage/migrations/001_init_core_tables.sql` | القيود والفهارس الأساسية |


## سجل الإصلاحات المنفذة بعد المراجعة

تم تنفيذ الإصلاحات التالية على نسخة العمل:

| المجال | التغيير |
|---|---|
| المصادقة | إضافة `app/auth.py` والتحقق من `DASHBOARD_API_TOKEN` عبر Bearer أو `X-API-Key` باستخدام مقارنة ثابتة الزمن، ثم حماية راوتري Dashboard وWorkflow بالكامل. |
| الجاهزية | تعديل `/ready` ليعيد HTTP 503 عند فشل startup أو توقف المحرك بدل إرجاع حالة تبدو جاهزة. |
| startup | تسجيل خطأ startup داخليًا مع إبقاء HTTP متاحًا للتشخيص، مع منع readiness الكاذب. |
| المسارات | إزالة تعريف `GET /` المكرر. |
| الأسرار | إضافة `config/settings.py` إلى `.gitignore` وإزالة الملف من فهرس Git مع إبقاء النسخة المحلية، وإضافة `DASHBOARD_API_TOKEN` إلى `render.yaml`. |
| الاختبارات | إضافة `pytest-timeout` وmarker للتكامل، وإضافة `scripts/verify_repairs.py` للتحقق من حماية الراوتر. |
| الجودة | إصلاح استيراد `timedelta` المفقود في `portfolio/performance.py` وإزالة الاستيرادات غير المستخدمة من Workflow. |

### نتائج التحقق

نجح اختبار التحقق من الراوتر والمصادقة، ونجح `compileall`، ونجحت اختبارات `tests/unit/test_portfolio.py` و`tests/unit/test_dashboard_endpoints.py` بعدد **9 اختبارات**، كما نجح Ruff على ملفات الإنتاج التي تم تعديلها. لم تُجرَ إعادة كتابة لتاريخ Git؛ لذلك يجب تدوير الأسرار القديمة وتنظيف التاريخ بشكل مستقل قبل النشر.
