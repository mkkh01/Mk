# تقرير التدقيق الجذري لنظام CT

## نطاق التدقيق

تمت مطابقة الكود في `engine/orchestrator.py` و`engine/htf_filter.py` و`engine/confidence.py` و`engine/volume.py` مع حالة الخدمة المنشورة وبيانات Supabase الفعلية. اقتصرت عمليات Supabase على القراءة والاستعلامات المحدودة؛ لم تُعدّل أي بيانات أو جداول.

## النتيجة التنفيذية

الخدمة ليست متوقفة تقنيًا. آخر حالة منشورة أظهرت أن المحرك يعمل، وأنه حلل **432 دورة** لست عملات، وسجل **432 كتابة** في قاعدة البيانات، مع **صفر أخطاء وصفر تحذيرات**. ومع ذلك كانت النتيجة **0 صفقة معتمدة و432 قرارًا مرفوضًا**.

السبب الجذري ليس Volume State وحده. توجد مشكلتان منطقيتان أساسيتان تمنعان فرصًا صالحة وتشوّهان الثقة والـ HTF:

1. `primary_signal` يختار إشارة trend في الفريم الأقصر حتى لو كانت محايدة، ويتجاهل إشارة structure أو SMC صاعدة صالحة في الفريم نفسه.
2. `_check_alignment` يسمح بمرور `LTF neutral` قبل فحص HTF، ولذلك يمكن أن يظهر `htf_bias=bearish` مع `htf_alignment=true`. هذا يجعل الاتجاه الهابط يبدو متوافقًا ويضيف وزن HTF إلى الثقة بطريقة غير صحيحة.

يوجد خلل ثالث في القياس: `score` يُحسب من كل الإشارات الصاعدة عبر جميع الفريمات، بينما `primary_direction` يعتمد عمليًا على إشارة LTF واحدة. لذلك تظهر قرارات مثل `score=0.80` مع `primary_direction=neutral`، وهي أرقام تبدو متناقضة للمستخدم لكنها ناتجة عن مسارين مختلفين للحساب.

## الأدلة من Supabase

جميع العملات الست مفعلة وتستخدم الفريمات نفسها: `15m`, `1h`, `4h`. رأس المال المخزن لكل عملة هو `30`، ونسبة المخاطرة `2%`. أحدث الشموع المغلقة موجودة لكل هذه الفريمات؛ مثلًا شمعة 15m الأخيرة عند `21:00` أغلقت عند `21:14:59.999Z`، وشمعة 1h الأخيرة عند `20:00`، وشمعة 4h الأخيرة عند `16:00`. هذا يثبت أن مسار البيانات الحالي يعمل ولا يعاني من غياب الشموع المطلوبة للفريمات الفعالة.

في المقابل، توجد سجلات يومية قديمة عند `2026-08-07` رغم أن وقت التدقيق `2026-08-15`. الفريم اليومي ليس ضمن إعداد العملات الحالي، ولذلك لا يبدو أنه سبب الرفض الحالي، لكنه يمثل مشكلة نظافة بيانات يجب منعها بقاعدة freshness وعدم السماح لأي تحليل باستخدام فريم أقدم من حد زمني مقبول.

أحدث القرارات تثبت أن النظام يصل إلى القرار النهائي ويخزنه، لكنها ترفضه قبل التنفيذ. أمثلة حديثة:

| الرمز | score | confidence | الحالة | السبب الظاهر |
|---|---:|---:|---|---|
| DOTUSDT | 0.800 | 0.600 | مرفوض | الثقة أقل من 0.65 |
| LINKUSDT | 0.759 | 0.466 | مرفوض | الثقة أقل من 0.65 |
| ADAUSDT | 0.685 | 0.580 | مرفوض | الثقة أقل من 0.65 |
| XRPUSDT | 0.659 | 0.467 | مرفوض | الثقة أقل من 0.65 |
| NEARUSDT | 0.642 | 0.242 | مرفوض | الثقة أقل من 0.65 |

لكن قرارات أخرى ذات confidence أعلى من `0.65` رُفضت بسبب `primary_direction=neutral` أو `volume_state=bearish`. هذا يؤكد أن خفض حد الثقة وحده لن يحل المشكلة.

## السبب الجذري الأول: اختيار primary signal غير صحيح

في `engine/orchestrator.py`، الدالة `_pick_primary_signal` تطبق هذا الترتيب:

```text
trend في LTF
ثم momentum في LTF
ثم أول إشارة LTF
```

المشكلة أن وجود trend signal لا يعني أنه صالح كإشارة اتجاه. إذا كانت إشارة trend محايدة، تُعاد مباشرة ولا يُسمح لإشارة structure أو SMC صاعدة أن تصبح الاتجاه الأساسي.

ظهر ذلك فعليًا في قرار DOT الأخير. في فريم 15m كانت structure هابطة ومحايدة، وtrend هابط ومحايد، لكن volume صاعد. وفي فريم 1h كانت structure وSMC صاعدتين، إلا أن قرار Long النهائي بقي محكومًا بإشارة LTF المحايدة. وفي LINK كان `structure_trend=up` على 15m، لكن trend أعاد `direction=neutral` بسبب أن EMA alignment غير حاسم؛ النتيجة أن `primary_direction` بقي `neutral` رغم وجود بنية صاعدة.

هذا تصميم متشدد ومضلل: **إشارة trend محايدة تُعامل كأنها صاحبة أولوية على إشارة structure صاعدة**، بدل أن تعني فقط أن trend لم يحسم رأيه.

## السبب الجذري الثاني: خطأ HTF alignment

في `engine/htf_filter.py`، دالة `_check_alignment` تبدأ بهذا الشرط:

```python
if ltf_direction == "neutral":
    return True, "ltf_neutral_pass_through"
```

قبل أن تتحقق من أن HTF هابط. لذلك إذا كان:

```text
LTF direction = neutral
HTF bias = bearish
```

فسترجع الدالة `alignment=True`. هذا ظهر في Supabase بصورة مباشرة: عدة قرارات تحمل:

```text
htf_bias_aligned = true
htf_bias = bearish
htf_ok = true
```

هذا غير مناسب لاستراتيجية Spot Long. الحياد في LTF لا يجب أن يجعل سياقًا هابطًا متوافقًا. النتيجة الحالية أن `htf_result.alignment` يدخل في حساب الثقة كعنصر إيجابي، رغم أن `htf_bias` هابط.

التصحيح المنطقي هو:

```text
HTF bearish + LTF long    => reject
HTF bearish + LTF neutral => reject أو watchlist، وليس aligned
HTF neutral + LTF neutral => pass-through
HTF bullish + LTF neutral => watchlist أو pass بشروط أخرى
HTF bullish + LTF long    => aligned
```

## السبب الجذري الثالث: انفصال score عن الاتجاه الحقيقي

الدالة `aggregate_directional_score` تحسب متوسط scores لكل الإشارات التي اتجاهها `long` عبر **جميع الفريمات**. أما `primary_direction` فيأتي من إشارة LTF واحدة. بالتالي يمكن أن تعطي 1h و4h إشارات Long مرتفعة، فيرتفع score، بينما تمنع إشارة trend المحايدة في 15m القرار النهائي.

هذا هو سبب ظهور حالات مثل:

```text
score = 0.800
primary_direction = neutral
confidence = 0.600
```

المشكلة ليست أن الرقم الحسابي مستحيل، بل أن النظام يستخدمه كأنه مقياس واحد للقرار بينما هو يقيس شيئًا مختلفًا. يجب فصل المؤشرات بوضوح:

| المؤشر | ما يجب أن يقيسه |
|---|---|
| `context_score` | توافق 4h و1h |
| `entry_score` | قابلية الدخول على 15m |
| `confidence` | احتمال جودة القرار بعد العقوبات |
| `final_direction` | اتجاه واحد محسوم بعد دمج الفريمات |

ولا ينبغي السماح لـ `score` العام بتجاوز اتجاه LTF أو HTF.

## السبب الجذري الرابع: الثقة ليست معايرة اتجاهية بما يكفي

`calculate_confidence` يستخدم `htf_result.alignment` كقيمة ثنائية، لا `htf_bias` الفعلي. وبسبب خطأ pass-through السابق، يمكن أن يحصل قرار مع HTF هابط على مساهمة HTF كاملة. كما أن `volume_confirmation` المستخدم في الثقة مختلف عن `ltf_volume_score` المستخدم في بوابة الجودة؛ لذلك قد يظهر `volume_score=0` مع `volume_confirmation` قريب من `0.5` أو أعلى.

هذا يخلق ازدواجية:

```text
volume_score       = بوابة اتجاهية صلبة
volume_confirmation = مكوّن ناعم داخل confidence
```

الفصل مقبول من حيث المبدأ، لكن يجب ألا يسمح المكوّن الناعم بتعويض تعارض HTF أو اتجاه LTF. الثقة الحالية ليست calibrated probability؛ هي weighted score، ولا ينبغي تفسير `0.65` كاحتمال نجاح 65%.

## السبب الجذري الخامس: البيانات تعمل، لكن freshness غير محمي

الشموع الفعالة حديثة ومغلقة، وهذا يستبعد توقف ingestion كسبب مباشر في اللحظة التي تم فحصها. لكن وجود شموع يومية قديمة حتى `2026-08-07` يدل على أن طبقة البيانات لا تطبق freshness validation على كل الفريمات. إذا دخل فريم قديم إلى التحليل مستقبلًا، فقد ينتج اتجاهًا غير حقيقيًا دون أن يظهر كخطأ تقني.

يجب أن يرفض التحليل أو يحوله إلى `stale_data` عندما يتجاوز عمر آخر شمعة مغلقة حدًا خاصًا بكل فريم، مثل:

| الفريم | الحد الأقصى المقترح للتأخر |
|---|---:|
| 15m | 30–45 دقيقة |
| 1h | 90–120 دقيقة |
| 4h | 5 ساعات |
| 1d | 26–30 ساعة |

## ما ليس سببًا جذريًا

Volume State كان متشددًا، وتم تحسينه بحيث لا يحظر `neutral` وحده. لكنه ليس سبب المشكلة الوحيد؛ الأدلة الحالية تُظهر أن كثيرًا من القرارات تُحظر أصلًا بسبب `primary_direction=neutral` أو confidence منخفضة بسبب HTF/structure/momentum. كذلك لا توجد مشكلة DB write في snapshot الحالي، وعدد الكتابات يطابق عدد التحليلات.

كما أن عبارة `System Health: EXCELLENT` صحيحة من منظور توفر الخدمات، لكنها لا تقيس جودة الاستراتيجية. يجب فصل `infrastructure_health` عن `strategy_activity` حتى لا تظهر الخدمة ممتازة بينما `approved=0` لعدة ساعات.

## ترتيب الإصلاحات

### P0 — إصلاح HTF alignment

يجب تعديل `_check_alignment` بحيث لا يمرر `LTF neutral` عندما يكون `HTF bearish`. هذا إصلاح correctness وليس تخفيفًا للفلتر، ويمنع احتساب الاتجاه الهابط دعمًا للثقة.

### P0 — إعادة بناء اختيار الاتجاه الأساسي

يجب ألا تعود الدالة trend المحايد مباشرة. المقترح هو حساب اتجاه LTF بهذه الأولوية:

```text
إذا structure وSMC أو momentum يدعمان Long مع عدم وجود trend هابط مؤكد => long_candidate
إذا trend هابط مؤكد أو CVD/Delta هابطان معًا => neutral/bearish
إذا الأدلة متعارضة => neutral
```

ولا يُسمح بتحويل neutral إلى long إلا عند وجود حد أدنى من مكونات مستقلة، مثل structure أو SMC مع volume/ momentum داعم.

### P0 — توحيد score والقرار

يجب حساب score منفصل للسياق والدخول، ثم استخدام `final_direction` واحد في HTF وconfidence وquality gate. لا ينبغي أن يعطي 1h و4h score مرتفعًا لقرار بينما 15m neutral دون أن يظهر ذلك صراحة في التشخيص.

### P1 — إضافة freshness gate

يجب تسجيل `data_age_seconds` لكل فريم، ومنع استخدام أي فريم stale. كما يجب إزالة أو تحديث شموع 1d القديمة أو منعها من دخول التحليل.

### P1 — تحسين لوحة CT

يجب عرض الحقول التالية في كل قرار:

```text
htf_bias
htf_alignment_reason
primary_signal_strategy
primary_direction
context_score
entry_score
volume_state
confidence_before_cap
confidence_after_cap
final_rejection_gate
```

ويجب أن يظهر `htf_bias=bearish` كحالة هابطة حتى لو كان LTF محايدًا، لا أن يظهر `htf_ok=true` فقط.

### P1 — فصل الصحة التشغيلية عن النشاط الاستراتيجي

أضف حالة مستقلة مثل:

```text
Infrastructure: EXCELLENT
Market Data: FRESH
Strategy Activity: BLOCKED_BY_DIRECTION
Trade Activity: NO_APPROVALS
```

## ملاحظات أمنية من Supabase

فحص مخطط Supabase أظهر أن RLS معطل على 10 جداول عامة، منها `candles`, `decisions`, `simulated_trades`, `coins`, `signals`, `trade_results`, و`logs`. هذا لا يفسر غياب الصفقات، لكنه خطر أمني حرج: أي عميل يملك anon key قد يستطيع قراءة أو تعديل هذه البيانات بحسب طريقة الوصول. لم أفعّل RLS تلقائيًا لأن تفعيله بلا policies قد يوقف التطبيق. يجب تصميم policies مناسبة ثم تفعيل RLS في migration منفصلة.

## الخلاصة

الخلل الجذري الأوضح هو أن النظام لا يبني قرار الاتجاه من توافق الفريمات؛ بل يختار trend محايدًا من 15m كـ primary signal، ويمرر في الوقت نفسه HTF هابطًا كأنه aligned بسبب قاعدة `ltf_neutral_pass_through`. بعد ذلك يُحسب score من إشارات Long عبر فريمات مختلفة، فيظهر score مرتفعًا لكنه لا يقود إلى قرار Long فعلي.

لذلك لا أوصي الآن بخفض الثقة أو إلغاء فلاتر الحجم. الإصلاح الصحيح يبدأ بـ **HTF alignment، ثم اختيار primary direction، ثم توحيد score/confidence**. بعد هذه الإصلاحات فقط يمكن إعادة تقييم عدد الصفقات ونسبة النجاح على replay وpaper trading.

لم تُجرَ أي تغييرات على كود الإنتاج أو بيانات Supabase أثناء هذا التدقيق.
