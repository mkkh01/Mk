# =====================================================
#  Render Blueprint - النشر بضغطة واحدة
#  Dashboard ← New ← Blueprint ← اختر المستودع
#
#  ملاحظة مهمة: الخطة المجانية (free) تُنام بعد 15 دقيقة
#  خمول وتوقف الدورات! للدورة كل دقيقة 24/7 استخدم starter.
# =====================================================
services:
  - type: web
    name: paper-trading-bot
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      # --- أسرار (املأها من لوحة Render بعد النشر) ---
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_KEY
        sync: false
      - key: REDIS_URL
        sync: false
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: TELEGRAM_CHAT_ID
        sync: false
      - key: WEBHOOK_SECRET
        generateValue: true
      - key: WEBHOOK_BASE_URL
        sync: false
      # --- ضبط (يمكن تعديله لاحقاً) ---
      - key: CYCLE_SECONDS
        value: 60
      - key: MONITOR_SECONDS
        value: 5
      - key: WS_ENABLE
        value: 1
      - key: HTF
        value: 1h
      - key: MTF
        value: 15m
      - key: LTF
        value: 5m
      - key: START_BALANCE
        value: 10000
      - key: RISK_PCT
        value: 1.0
      - key: LEVERAGE
        value: 1
      - key: MAX_OPEN_TRADES
        value: 3
      - key: MIN_SCORE
        value: 70
      - key: WATCH_SCORE
        value: 60
      - key: RR_MIN
        value: 1.3
      - key: ATR_SL_MULT
        value: 0.2
      - key: SLIPPAGE_BPS
        value: 5
      - key: COOLDOWN_MINUTES
        value: 30
      - key: ALLOW_SHORTS
        value: true
      - key: FEE_PCT
        value: 0.1
      - key: MAX_HOLD_HOURS
        value: 48
      - key: DATA_SOURCES
        value: binance,okx,gate,kucoin

# --- بديل: Redis مدفوع من Render نفسها (اختياري) ---
# إذا أردت Redis من Render بدل Upstash المجاني، أضف:
# databases:
#   - name: trading-redis
#     plan: starter
# ثم اربطه: REDIS_URL به fromDatabase (راجع README).
