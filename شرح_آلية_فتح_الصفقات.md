# شرح آلية فتح الصفقات في النظام (بعد الإصلاح)

هذا التوثيق يشرح، خطوة بخطوة، كيف يفتح النظام صفقة جديدة حالياً، بدءاً من وصول الشمعة من Binance وحتى تسجيل الصفقة في قاعدة البيانات وإرسال إشعار تيليجرام. المسار يمر عبر أربع طبقات: **الاستقبال → اتخاذ القرار → التنفيذ → المراقبة**.

---

## 1. الاستقبال: كيف تصل الشمعة إلى المحلل (Ingest)

| المرحلة | التفاصيل |
|---------|----------|
| WebSocket من Binance | يتصل `ingest/binance_ws.py` بـ WebSocket العميق للشموع (`kline`) لكل رمز وإطار زمني مفعّل (مثل `XLMUSDT@kline_15m`). |
| التحقق من الإغلاق | لا تُعالج إلا الشموع **المغلقة فعلياً** (`is_closed=True`)، حتى لا يتخذ النظام قراراً من شمعة لا تزال تتحرك. |
| التخزين | تُكتب الشمعة في Supabase (جدول `candles`) وتُنشر على قناة Redis `new_candle:{symbol}:{timeframe}`. |
| السعر الحي | مع كل تيك (محدّث live)، يُحدَّث مفتاح `live_price:{symbol}` في Redis بتاريخ ووقت محدّث — **هذا هو المصدر الذي يعتمد عليه سعر التنفيذ الآن** (مهلة قصوى 60 ثانية). |

---

## 2. اتخاذ القرار: دورة التحليل عند كل شمعة مغلقة (`engine/orchestrator.py → process_candle`)

عند وصول شمعة مغلقة لرمز معين، يمر التحليل بسلسلة **بوابات متتالية (gates)**؛ أي فشل في بوابة يوقف العملية ويُسجَّل سبب الرفض:

### البوابة 1 — تحليل الفريم المنخفض (LTF)
تُحلَّل الشموع (آخر عدد حسب الإطار الزمني) لاستخراج:
- **الاتجاه**: EMA20/EMA50 و ADX و قوة الاتجاه.
- **البنية السوقية**: BOS / CHOCH ونقاط التأرجح (Swings).
- **Smart Money**: Order Blocks و Fair Value Gaps غير المملوءة/غير المخففة.
- **الزخم**: RSI / MACD / Stochastic.
- **التقلّب**: ATR.
- **الجلسة الزمنية**: Asian / London / NY (يؤثر على权重 الثقة).
- **حجم التداول**: CVD slope.

### البوابة 2 — الفلتر متعدد الفريمات (HTF)
يُتحقق من أن اتجاه الفريم العالي (مثل 4h أو 1d) **متوافق** مع اتجاه الإشارة — لا يُفتح Long في اتجاه هابط بالفريم الكبير.

### البوابة 3 — فحص regime والبنية
يُتحقق من حالة السوق (Trending / Ranging / Volatile) ومن محاذاة البنية السوقية (BOS/CHOCH مؤكدة).

### البوابة 4 — الثقة (Confidence)
تُحسب ثقة نهائية موزونة (HTF 20% + Structure 30% + Momentum 20% + Liquidity 25% + Session 5%) وتُعدَّل حسب regime. يجب أن تتجاوز `CONFIDENCE_THRESHOLD = 0.60`.

### البوابة 5 — إدارة المخاطر (`engine/risk.py → assess_risk`)
فحوصات تُنفَّذ **بالتسلسل وأول فشل ينتهي فوراً**:

| الفحص | الشرط | القيمة الافتراضية |
|-------|-------|-------------------|
| صفقة مفتوحة لنفس الرمز | لا يُسمح | 1 صفقة لكل رمز فقط |
| الاتجاه | `long` فقط (Spot) | — |
| نسبة المكافأة للمخاطرة | `R:R >= 1.4` | `MIN_RISK_REWARD_RATIO` |
| الصفقات المتزامنة | `< 10` | `MAX_CONCURRENT_TRADES` |
| التعرض | لا يتجاوز 100% من رأس المال | `MAX_PORTFOLIO_EXPOSURE_PCT` |
| الدرواداوان اليومي | `< 9%` من ذروة الربح | `MAX_DAILY_LOSS_PCT` |

إذا نجت كل الفحوصات تُحسب:
- **SL** = `entry − ATR × 1.8` (multiplier افتراضي)
- **TP** = `entry + ATR × 3.2`
- **الحجم** = `(رأس المال × نسبة المخاطرة) ÷ (entry − SL)` بحيث يكون المبلغ المعرض للخسارة ثابتاً بالـ USDT.

### البوابة 6 — تنقيح نقطة الدخول (`engine/entry_rules.py → refine_entry`)
تُقرر الدالة نوع الدخول:
- **Limit**: إذا كان السعر الحالي قريباً (≤ 5%) من Order Block غير مخفَّف أو حافة FVG غير مملوءة → الدخول عند مستوى OB/FVG مع إزاحة تفضيلية `ENTRY_LIMIT_OFFSET_PCT = 3%` (سعر أفضل للمشتري).
- **Market**: لا يوجد مستوى قريب → الدخول بالسعر الحالي.
- تُحسب SL/TP مجدداً حول سعر الدخول النهائي، وتُحدَّد صلاحية الإشارة `ENTRY_TIMEOUT_MINUTES = 20` دقيقة.

### ناتج الدورة
إذا اجتازت كل البوابات يُبنى `DecisionResult(final_verdict=True)` مع `EntrySignal` كامل (سعر الدخول، SL، TP، الحجم، السبب، الثقة)، ويُكتب في جدول `decisions` (idempotent).

---

## 3. التنفيذ: فتح الصفقة (`app/main.py` → `PaperTrader.open_trade`)

مسؤولية الفتح الفعلي موجودة **حصرياً** في `app/main.py` (وليس في orchestrator، لتجنب التكرار):

```
result.final_verdict == True و result.entry موجودة
        │
        ▼
PaperTrader(self._supabase, redis=self._redis)   ← مع تمرير Redis (بعد الإصلاح)
        │
        ▼
trader.open_trade(decision)
```

### خطوات `open_trade` (بعد الإصلاح `17f971b`):

| # | الخطوة | التفاصيل |
|---|--------|----------|
| 1 | استخراج بيانات الإشارة | `signal_price` = سعر الدخول من الإشارة (يُحفظ للمقارنة)، الحجم من `risk.max_position_size`، SL/TP من `entry`. |
| 2 | **تحديد سعر التنفيذ الفعلي** | أولاً `live_price:{symbol}` من Redis (عمره < 60 ثانية فقط). إذا لم يوجد → آخر شمعة مغلقة في قاعدة البيانات. إذا لم يوجد → سعر الإشارة نفسه (Fallback). |
| 3 | إزاحة SL/TP | إذا اختلف سعر التنفيذ عن سعر الإشارة، تُزاح المستويات بنفس **المسافة المطلقة** فيبقى مبلغ المخاطرة بالـ USDT ونسبة R:R كما خططت. مثال: إشارة عند 100 وSL عند 95 وTP عند 110، تنفيذ عند 102 → SL=97 وTP=112. |
| 4 | إعادة تحجيم الكمية | الكمية الجديدة = `(الكمية × سعر الإشارة) ÷ سعر التنفيذ` — نفس قيمة المبلغ بالـ USDT بالضبط. |
| 5 | الرسوم والانزلاق | تُحسب على سعر التنفيذ الفعلي: رسوم taker 0.01% + انزلاق 0.05%. |
| 6 | ATR عند الدخول | يُحسب من آخر ~19 شمعة مغلقة لبدء آلية trailing-stop. |
| 7 | التسجيل في DB | يُدخل الصفقة في `simulated_trades` (idempotent على `decision_id` — لا تكرار أبداً)، مع حفظ `signal_price` و`live_price_age_seconds` للشفافية. |
| 8 | الإشعار | تُرسل رسالة تيليجرام بتفاصيل الصفقة (السعر، SL، TP، الثقة). |

---

## 4. المراقبة بعد الفتح (دورة الإغلاق)

- **فحص الإغلاق**: عند كل شمعة مغلقة جديدة، يفحص `check_trade_closure` كل الصفقات المفتوحة: إذا لمس close/high/low للشمعة SL أو TP تُغلق الصفقة ويُحسب PnL صافي الرسوم والانزلاق.
- **Trailing Stop**: إذا فُعِّل، وبعد بلوغ الربح غير المحقق `1.5 × المخاطرة الأولية`، يبدأ وقف الخسارة بالتتبع خلف أعلى سعر مسجل (`highest_price − max(ATR×2.2, 0.5% من السعر)`، مع سقف 5% ودنيا 0.5%)، ولا يتحرك أبداً للخلف.
- **فحص دوري**: بالإضافة إلى فحص الشموع، يوجد thread دوري يمسح كل الصفقات المفتوحة كل فترة ويحدّث trailing stops.
- **إيقاف المحرك**: عند توقف المحرك تُغلق كل الصفقات المفتوحة (close-all) بسعر آخر شمعة.

---

## ملخص بصري للتدفق الكامل

```
Binance kline WS (شمعة مغلقة)
        │
        ▼
redis new_candle:{sym}:{tf}  →  app/main.process_candle
        │
        ▼
orchestrator.process_candle ──→ [G1: LTF analysis] → [G2: HTF] → [G3: regime/structure]
        │                                                → [G4: confidence ≥ 0.60]
        ▼
risk.assess_risk ──→ [1 صفقة/رمز, long فقط, R:R≥1.4, ≤10 صفقات,
                      تعريض ≤100%, درو ≤9%] → حجم + SL/TP بالـ ATR
        │
        ▼
refine_entry ──→ Limit (OB/FVG ±3%) أم Market (السعر الحالي)
        │
        ▼ DecisionResult(final_verdict=True)
app/main ──→ PaperTrader.open_trade(decision)
        │
        ▼
سعر تنفيذ فعلي (Redis live ≤60ث ← آخر شمعة DB ← سعر الإشارة)
        │
        ▼
shift SL/TP + rescale size + fees on fill price
        │
        ▼
simulated_trades (DB) + تيليجرام
        │
        ▼
مراقبة: إغلاق عند SL/TP + Trailing Stop
```

---

## ما الذي تغيّر بعد إصلاح سعر الدخول (`17f971b`)

| قبل | بعد |
|-----|-----|
| سعر الدخول = سعر الإشارة حرفياً (آخر close في DB لحظة الإشارة) | سعر الدخول = سعر السوق الحقيقي لحظة التنفيذ (Redis live، عمره < 60 ثانية) |
| عدة صفقات متباعدة زمنياً قد تُفتح بنفس السعر القديم | كل صفقة تُفتح بسعرها الزمني الصحيح |
| لا فرق بين سعر الإشارة والتنفيذ | `signal_price` و`live_price_age_seconds` يُسجَّلان للمقارنة والشفافية |
| SL/TP والحجم والرسوم محسوبة على السعر القديم | تُعاد إزاحة SL/TP وتحجيم الحجم وحساب الرسوم على السعر الفعلي، مع الحفاظ على مبلغ المخاطرة بالـ USDT |
