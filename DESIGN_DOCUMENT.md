# تصميم طبقة المراقبة التشغيلية وحزمة تحليل نتائج الصفقات

## 1. الملخص التنفيذي

تهدف هذه الوثيقة إلى تقديم تصميم مفصل لطبقة المراقبة التشغيلية (Runtime Observability Layer) وحزمة تحليل نتائج الصفقات (Trade Analysis Results Package) ضمن مشروع CT. الهدف الأساسي هو توفير رؤية حية ودقيقة لأداء النظام، والكشف الفوري عن أي خلل أو تباطؤ، بالإضافة إلى عرض نتائج تحليل الصفقات بشكل واضح وديناميكي للمستخدم النهائي. سيتم التركيز على فصل الاهتمامات، وضمان عدم خلط منطق العمل مع منطق العرض، وتوفير سجلات قابلة للتتبع.

## 2. تحليل البنية الحالية (موجز)

بعد مراجعة الملفات الرئيسية في المشروع، تم تحديد البنية الحالية والمكونات الأساسية. الملف `app/main.py` هو نقطة الدخول الرئيسية، ويقوم بتوصيل المكونات المختلفة مثل Redis و Supabase و Telegram Bot و Orchestrator و Binance WebSocket. يحتوي هذا الملف على منطق بدء وإيقاف المحرك، ويدير المهام الخلفية. يوجد به حاليًا آلية بسيطة لتسجيل الحالة الصحية تعتمد على متغير `_health_stats` وحلقة تسجيل صحة دورية تعتمد على دالة `format_cycle_summary`.

الملف `engine/orchestrator.py` هو المحرك الرئيسي لتحليل الشموع واتخاذ القرارات. يستهلك البيانات من كائنات `Candle` ويُنتج كائنات `DecisionResult`. يقوم بتسجيل خطوات التحليل باستخدام وظائف من `monitoring/workflow_logger.py`. الملف `monitoring/logger.py` يوفر مسجل `structlog` مهيأ لإخراج سجلات JSON منظمة، ويحتوي على كتالوج للأحداث المسجلة حاليًا. الملف `monitoring/workflow_logger.py` يوفر وظائف مساعدة لتسجيل أحداث سير العمل بشكل منظم.

الملف `ingest/binance_ws.py` مسؤول عن استيعاب بيانات WebSocket من Binance، ويحتوي على منطق الكشف عن توقف البيانات وإعادة الاتصال. الملف `storage/supabase.py` هو طبقة الوصول إلى قاعدة البيانات لـ Supabase، ويقوم بتخزين الشموع والقرارات والصفقات المحاكية. الملف `bot/telegram_bot.py` هو واجهة المستخدم للروبوت، ويعرض حاليًا ملخصات بسيطة للصفقات وأداء المحفظة. الملف `app/dashboard_endpoints.py` يعرض نقاط نهاية لواجهة لوحة تحكم بسيطة، تعكس حالة النظام الصحية الحالية بشكل مباشر من `_health_stats`. الملف `app/workflow_endpoints.py` يبدو أنه ملف قديم أو غير مكتمل، يحتوي على نقاط نهاية متكررة ومنطق غير متوافق مع العقود الحالية.

**القضايا المعمارية الحالية:**

تعتمد المراقبة الحالية بشكل كبير على متغير `_health_stats` في `app/main.py`، وهي لا توفر رؤية عميقة لحالة كل مكون على حدة. كما أن الكشف عن "الصمت" يعتمد بشكل أساسي على `ingest/binance_ws.py`، ولا يوجد نظام شامل لمراقبة صحة جميع المكونات. عرض نتائج الصفقات في `bot/telegram_bot.py` محدود ولا يوفر تفاصيل كافية حول أسباب الدخول أو الرفض، أو الاستراتيجيات المستخدمة، أو المخاطر.

## 3. التصميم المقترح للطبقات

سيتم تقسيم العمل إلى طبقتين رئيسيتين: طبقة المراقبة التشغيلية وحزمة تحليل نتائج الصفقات.

### 3.1. طبقة المراقبة التشغيلية (Runtime Observability Layer)

ستكون هذه الطبقة مسؤولة عن مراقبة صحة جميع مكونات النظام بشكل مستمر، وتسجيل الأحداث الصحية، وإصدار إشارات "نبض القلب" (Heartbeat) الدورية.

**المكونات الرئيسية:**

*   **مدير الصحة (Health Manager):** مكون مركزي يتتبع حالة جميع المكونات الفرعية. سيقوم بتحديث حالة كل مكون بناءً على الأحداث المستلمة.
*   **مراقبو المكونات (Component Monitors):** فئات صغيرة مسؤولة عن مراقبة مكونات محددة (مثل WebSocket، Redis، Supabase، Orchestrator). ستقوم هذه الفئات بإرسال تحديثات الحالة إلى مدير الصحة.
*   **مسجل الأحداث الصحية (Health Event Logger):** مكون مسؤول عن تسجيل الأحداث الصحية (INFO, WARNING, ERROR, CRITICAL) بشكل منظم.
*   **مولد نبض القلب (Heartbeat Generator):** مهمة خلفية تقوم بإصدار سجل نبض قلب دوري يحتوي على ملخص لحالة النظام.

### 3.2. حزمة تحليل نتائج الصفقات (Trade Analysis Results Package)

ستكون هذه الحزمة مسؤولة عن تجميع وعرض نتائج تحليل الصفقات بشكل منظم وديناميكي.

**المكونات الرئيسية:**

*   **مجمع النتائج (Result Aggregator):** مكون يقوم بجمع بيانات القرارات والصفقات من قاعدة البيانات أو من الأحداث الحية.
*   **منسق النتائج (Result Formatter):** مكون يقوم بتنسيق البيانات المجمعة في أشكال مختلفة (نص، JSON، رسائل Telegram).
*   **محلل الأداء (Performance Analyzer):** مكون يقوم بحساب الإحصائيات والملخصات (مثل نسبة النجاح، متوسط العائد، أكثر الاستراتيجيات فعالية).

## 4. أسماء الملفات الجديدة ومسؤولياتها

| اسم الملف | المسؤولية |
| :--- | :--- |
| `monitoring/health_manager.py` | إدارة حالة صحة النظام المركزية، وتتبع حالة المكونات الفردية. |
| `monitoring/component_monitors.py` | فئات لمراقبة مكونات محددة (WebSocket, Redis, Supabase, Orchestrator). |
| `monitoring/heartbeat.py` | توليد سجلات نبض القلب الدورية. |
| `analysis/result_aggregator.py` | تجميع بيانات القرارات والصفقات. |
| `analysis/result_formatter.py` | تنسيق نتائج التحليل للعرض. |
| `analysis/performance_analyzer.py` | حساب إحصائيات الأداء والملخصات. |

## 5. مسار البيانات من الاستقبال حتى العرض

1.  **استقبال البيانات:** يتم استقبال الشموع عبر `ingest/binance_ws.py`. يقوم مراقب WebSocket بتحديث حالة الاتصال في `Health Manager`.
2.  **التحليل والقرار:** يقوم `engine/orchestrator.py` بتحليل الشموع واتخاذ القرارات. يقوم مراقب Orchestrator بتحديث حالة حلقة التحليل في `Health Manager`.
3.  **التخزين:** يتم تخزين القرارات والصفقات في قاعدة البيانات عبر `storage/supabase.py`. يقوم مراقب Supabase بتحديث حالة الاتصال في `Health Manager`.
4.  **المراقبة الحية:** يقوم `Health Manager` بتجميع حالة جميع المكونات. يقوم `Heartbeat Generator` بإصدار سجل نبض قلب دوري. يتم تسجيل أي تغيير في حالة المكونات كحدث صحي (Health Event).
5.  **تجميع النتائج:** يقوم `Result Aggregator` بجمع بيانات القرارات والصفقات من قاعدة البيانات.
6.  **تحليل الأداء:** يقوم `Performance Analyzer` بحساب الإحصائيات والملخصات.
7.  **العرض:** يقوم `Result Formatter` بتنسيق النتائج والإحصائيات. يتم عرض النتائج عبر `bot/telegram_bot.py` أو عبر نقاط نهاية API.

## 6. شكل السجلات الحية (Heartbeat & Health Events)

**سجل نبض القلب (Heartbeat):**

سيكون سجل نبض القلب عبارة عن رسالة JSON منظمة تحتوي على معلومات حول وقت التشغيل، عدد الدورات، الشموع المستلمة، التحليلات المنفذة، الإشارات الصادرة، الصفقات المحاكية، عدد الأخطاء والتحذيرات، وآخر نشاط.

**الأحداث الصحية (Health Events):**

ستكون الأحداث الصحية عبارة عن رسائل JSON منظمة تحتوي على مستوى الحدث (INFO, WARNING, ERROR, CRITICAL)، المكون المتأثر، رسالة وصفية، وتفاصيل إضافية.

## 7. شكل عرض نتائج الصفقات

سيتم عرض نتائج الصفقات بشكل منظم يتضمن معلومات حول العملة، الإطار الزمني، وقت الدخول والخروج، اسم الاستراتيجية، حالة السوق، النتيجة النهائية، الثقة، المخاطرة، القرار، سبب الدخول أو الرفض، الربح/الخسارة، وحالة الصفقة (محاكية أو حية). سيتم توفير ملخصات لآخر 10 صفقات، أفضل وأسوأ الصفقات، نسبة النجاح، متوسط العائد والمخاطرة، وأكثر الاستراتيجيات فعالية.

## 8. اكتشاف الخلل ومنع السجلات المضللة

سيتم اكتشاف الخلل لحظة حدوثه من خلال مراقبة حالة المكونات الفردية. إذا توقف مكون عن إرسال تحديثات الحالة لفترة زمنية محددة، سيتم تسجيل حدث صحي (CRITICAL). سيتم التأكد من أن النظام ليس صامتًا وهو متعطل من خلال مراقبة نشاط حلقة التحليل وتدفق الشموع. سيتم منع تكرار السجلات المضللة من خلال تتبع حالة المكونات وعدم تسجيل نفس الخطأ بشكل متكرر إلا إذا تغيرت الحالة.

## 9. إصلاحات البنية الحالية

يجب إزالة الاعتماد المفرط على `_health_stats` في `app/main.py` واستبداله بـ `Health Manager` المركزي. يجب أيضًا مراجعة `app/workflow_endpoints.py` وإزالة النقاط المتكررة أو تحديثها لتتوافق مع العقود الحالية. يجب التأكد من أن جميع الأخطاء يتم تسجيلها بشكل صحيح مع تفاصيل كاملة (النوع، الرسالة، الوحدة، الدالة، تتبع المكدس، وقت الحدوث).
