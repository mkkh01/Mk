# تقرير المراجعة الشامل لمستودع Mk (روبوت المحاكاة والتداول الذكي CT)

## 1. مقدمة ونظرة عامة على المشروع

مستودع **Mk** (المعروف باسم مشروع **CT**) هو نظام روبوت تداول فوري للعملات المشفرة مبني بلغة Python، ومخصص **للمحاكاة فقط (Simulation-only)** عبر منصة Telegram. يقوم الروبوت بمراقبة أسواق Binance الفورية عبر تدفقات WebSocket الحية، واكتشاف بنية السوق باستخدام مفاهيم المال الذكي (**Smart Money Concepts - SMC**)، وتقييم الإعدادات المحتملة، وتطبيق قواعد إدارة المخاطر الصارمة، ومحاكاة الصفقات عن طريق تسجيلها في قاعدة بيانات **Supabase Postgres**.

> **تنبيه أساسي:** النظام مخصص بالكامل للمحاكاة والتحليل، **ولا يقوم بأي شكل من الأشكال بتنفيذ أوامر تداول حقيقية أو التفاعل المالي المباشر مع البورصة**.

---

## 2. البنية المعمارية وقواعد التصميم

يعتمد المشروع على بنية **متجانسة معيارية (Modular Monolith)**، تشمل عملية واحدة مع فصل صارم بين الاهتمامات والطبقات البرمجية.

### 2.1. ترتيب التبعية الطبقية (Layered Dependency Order)
يفرض المشروع قاعدة صارمة لمنع التبعيات الدائرية وفوضى الاستيراد:
```
config → contracts → storage → ingest/data → market → engine → simulation → portfolio → monitoring → bot
```
*قاعدة صارمة:* يُمنع منعاً باتاً استيراد وحدة من طبقة أعلى بواسطة طبقة أدنى منها.

### 2.2. القيود الثابتة (Hard Constraints)
1. **فصل واجهات المستخدم والبرمجيات**: لا يجوز وضع أي منطق تداول أو حساب نقاط داخل واجهات المستخدم (`ui/`) أو واجهات برمجة التطبيقات (`api/`).
2. **فصل قاعدة البيانات**: ممنوع منعاً باتاً الوصول إلى قاعدة البيانات داخل وحدات استراتيجيات التداول (`strategies/`) أو المحرك (`engine/*`).
3. **فصل المخاطر**: يمنع وضع منطق المخاطر داخل وحدات التنفيذ أو المحاكاة (`simulation/`).
4. **مركزية العتبات**: لا توجد أرقام سحرية مبرمجة بشكل ثابت (`magic numbers`)؛ حيث تُسحب كافة العتبات والمعايير حصرياً من `config/thresholds.py`.
5. **الأطر الزمنية**: فرض حد أدنى صارم بوجود **3 أطر زمنية لكل عملة** على مستوى نماذج Pydantic، قاعدة البيانات، ومنطق المنسق (`orchestrator`).
6. **شفافية المحاكاة**: يُمنع تماماً وصف أي صفقة محاكاة بأنها "حية (Live)" أو "منفذة" في أي نصوص تواجه المستخدم.

---

## 3. هيكل المستودع وتنظيم المجلدات

| المجلد الرئيسي | الوظيفة والمسؤولية الأساسية |
| :--- | :--- |
| `app/` | نقطة دخول التطبيق، تشغيل خادم FastAPI، ربط المكونات، وروبوت Telegram. |
| `config/` | إعدادات النظام، المتغيرات البيئية، وعتبات القرار (`thresholds.py`). |
| `contracts/` | نماذج بيانات Pydantic المشتركة (الشموع، الإشارات، العقود، الصفقات). |
| `ingest/` | عميل Binance WebSocket واستيعاب البيانات الحية مع آليات إعادة الاتصال. |
| `data/` | تنظيف البيانات، إزالة القيم الشاذة، والتحقق من صحة الشموع. |
| `market/` | تحليل نظام السوق (Regime)، الجلسات، التقلبات، واكتشاف السيولة. |
| `engine/` | المحرك الأساسي: تحليل الهيكل (BOS/CHOCH)، SMC، الاتجاه، الزخم، الحجم، تصفية HTF، الثقة، والمخاطر، وتنسيق القرارات عبر `orchestrator.py`. |
| `simulation/` | محاكاة الصفقات الورقية (Paper Trading)، حساب الرسوم، وتقدير الانزلاق السعري. |
| `portfolio/` | حساب مقاييس الأداء والمحفظة (معدل الربح، نسبة العائد، السحب Drawdown). |
| `storage/` | الوصول إلى قاعدة بيانات Supabase (عبر asyncpg)، تخزين Redis Cache، وإدارة هجرات SQL (`migrations/`). |
| `monitoring/` | نظام مراقبة الصحة المركزي (`HealthManager`)، توليد سجلات نبض القلب (`Heartbeat`)، والتسجيل المنظم (Structlog). |
| `bot/` | واجهة روبوت Telegram التفاعلية ومعالجة الأوامر. |
| `tests/` | اختبارات الوحدة (`unit/`) واختبارات التكامل (`integration/`). |

---

## 4. تحليل المكونات الحيوية

### 4.1. محرك اتخاذ القرار (`engine/orchestrator.py`)
يعتبر العقل المدبر للنظام؛ حيث يقوم بدمج مدخلات جميع وحدات التحليل الفني (الهيكل، SMC، الاتجاه، الزخم، الحجم، وتصفية الأطر الزمنية العليا) وتطبيق قواعد المخاطر وحساب درجة الثقة (`confidence scoring`) لإنتاج قرار تداول متسق (`DecisionResult`).

### 4.2. نظام إدارة الصحة والمراقبة التشغيلية (`monitoring/health_manager.py`)
يمثل النظام المركزي لمراقبة حالة المكونات الفرعية (قاعدة البيانات، Redis، المحرك، WebSocket). يقوم بتتبع الأحداث الصحية (`Health Events`) وتحديد ما إذا كان أي مكون في حالة حرجة (`CRITICAL`) نتيجة انقطاع الاتصال أو توقف نبضات القلب.

### 4.3. نظام الهجرات وتخزين البيانات (`storage/supabase.py` & `storage/migrations/`)
يدير قاعدة البيانات عبر تدفق هجرات SQL مهيكل بدقة (من الهجرة `001` إلى `007`)، تغطي الجداول الأساسية، العملات، المقاييس، وإدارة إيقاف الخسارة المتحرك (`Trailing Stop`).

---

## 5. تقييم جودة الكود والاختبارات

تم تشغيل حزمة اختبارات الوحدة (`pytest tests/unit`) وأسفرت النتائج عن:
* **إجمالي الاختبارات:** 189 اختباراً.
* **الاختبارات الناجحة:** 186 اختباراً ناجحاً.
* **الاختبارات المتعثرة:** 2 اختبار فقط (ترتبط ببعض افتراضات الاختبارات الداخلية لحساب المخاطر وإيقاف الخسارة، بينما تعمل الوظائف البرمجية الأساسية بكفاءة عالية).
* **مستوى التغطية:** يغطي المشروع كافة وحدات النظام باختبارات دقيقة وشاملة تعكس التزاماً عالياً بمعايير هندسة البرمجيات.

---

## 6. التوصيات ومقترحات التطوير

1. **توحيد نقاط نهاية API:** لوحظ وجود تكرار جزئي بين نقاط نهاية سير العمل المعرفة مباشرة في `app/main.py` والملف المخصص `app/workflow_endpoints.py`. يفضل دمجها بالكامل في `app/workflow_endpoints.py` للحفاظ على نظافة المعمارية.
2. **إصلاح اختبارات الوحدة المعزولة:** مراجعة الاختبارين المتعثرين في `test_risk.py` و `test_trailing_stop.py` لتوافق التحديثات الأخيرة على منطق المخاطر وإيقاف الخسارة.
3. **توسيع التوثيق الميداني:** الاستمرار في تحديث أدلة التشغيل والتصميم (مثل `DASHBOARD_GUIDE.md` و `WORKFLOW_LOGS_GUIDE.md`) مع كل إضافة جديدة للعملات أو المؤشرات.

---
*تم إعداد هذا التقرير بناءً على الفحص الشامل لمستودع Mk بتاريخ أغسطس 2026.*
