# تقرير مراجعة مستودع CT

## 1. نظرة عامة على المشروع

مشروع CT هو روبوت تداول فوري للعملات المشفرة يعمل على Telegram، مصمم للمحاكاة فقط. يراقب الروبوت أسواق Binance الفورية عبر WebSocket، ويكتشف هيكل السوق باستخدام مفاهيم المال الذكي (SMC)، ويقيم الإعدادات المحتملة، ويطبق قواعد المخاطر، ويحاكي الصفقات عن طريق كتابة الصفوف إلى Supabase Postgres. من المهم ملاحظة أنه **لا يقوم بتنفيذ أوامر تبادل حقيقية** [^1].

## 2. البنية المعمارية

يعتمد مشروع CT على بنية **متجانسة معيارية (modular monolith)**، حيث يتم تشغيله كعملية واحدة، ويديره مطور واحد، ويتحكم فيه روبوت Telegram واحد. تعتمد فلسفة البنية على الحفاظ على منطق القرار في `engine/orchestrator.py`، والحفاظ على الروبوت نحيفًا، وتخزين البيانات بنوعية محددة، وعدم الخلط بين المحاكاة والتنفيذ [^1].

### 2.1. ترتيب التبعية الطبقية

يتبع المشروع ترتيب تبعية صارمًا لضمان فصل الاهتمامات ومنع التبعيات الدائرية:

`config → contracts → storage → ingest/data → market → engine → simulation → portfolio → monitoring → bot`

القاعدة الأساسية هي أنه إذا كان الملف يحتاج إلى استيراد من طبقة أعلى من طبقته، فيجب إعادة تصميم الملف [^1].

### 2.2. القيود الصارمة

يفرض المشروع عدة قيود صارمة لضمان سلامة التصميم والتشغيل [^1]:

*   لا يوجد منطق تداول/تسجيل نقاط في واجهة المستخدم (`ui/`) أو واجهة برمجة التطبيقات (`api/`).
*   لا يوجد وصول إلى قاعدة البيانات داخل وحدات `strategies/` أو `engine/*`.
*   لا يوجد منطق مخاطر داخل وحدات التنفيذ/المحاكاة.
*   لا توجد عتبات مبرمجة بشكل ثابت؛ يجب سحبها من `config/thresholds.py`.
*   لا توجد استيرادات دائرية.
*   **الحد الأدنى 3 أطر زمنية لكل عملة**، يتم فرضها في Pydantic وعلى مستوى قاعدة البيانات وفي منطق المنسق.
*   لا يتم أبدًا وصف صفقة محاكاة بأنها "حية" أو "منفذة" في أي نص يواجه المستخدم.
*   يجب تقييم كل عملة مضافة عبر 3 أطر زمنية على الأقل في وقت واحد.

## 3. هيكل المشروع

يتكون المشروع من عدة أدلة رئيسية، لكل منها مسؤولياته [^1]:

| الدليل | المسؤولية الرئيسية |
| :--- | :--- |
| `app/` | نقطة الدخول الرئيسية، ربط المكونات، نقطة دخول روبوت Telegram، تشغيل وإيقاف المحرك. |
| `config/` | إعدادات النظام والعتبات (thresholds). |
| `contracts/` | نماذج Pydantic للبيانات المشتركة (الشموع، القرارات، المخاطر، إلخ). |
| `ingest/` | استيعاب البيانات من Binance WebSocket. |
| `data/` | تنظيف البيانات والتحقق من صحتها. |
| `market/` | اكتشاف نظام السوق، الجلسات، التقلبات، والسيولة. |
| `engine/` | اكتشاف هيكل السوق، مفاهيم المال الذكي (SMC)، الاتجاه، الزخم، الحجم، تصفية الأطر الزمنية العليا، الثقة، المخاطر، قواعد الدخول، والتنسيق (orchestration). |
| `simulation/` | محاكاة الصفقات، حساب الرسوم، وتقدير الانزلاق. |
| `portfolio/` | حساب مقاييس الأداء (معدل الربح، الربح والخسارة، السحب). |
| `storage/` | الوصول إلى قاعدة البيانات (Supabase) و Redis Cache، وإدارة الهجرات. |
| `monitoring/` | التسجيل المنظم، إدارة الصحة، ومراقبة سير العمل. |
| `bot/` | واجهة روبوت Telegram. |
| `tests/` | اختبارات الوحدة والتكامل. |

## 4. تحليل المكونات الرئيسية

### 4.1. `app/main.py`

هذا الملف هو نقطة الدخول الرئيسية للتطبيق. يقوم بربط جميع الطبقات معًا، وتشغيل روبوت Telegram، وتشغيل محرك التحليل في الخلفية، وتنسيق الإغلاق السلس. يتضمن منطق بدء وإيقاف المحرك (`start_engine` و `stop_engine`)، والذي يقوم بإنشاء وتدمير كائنات `Orchestrator` و `BinanceWSClient` والمهام الخلفية. كما أنه مسؤول عن تحديث حالة `engine_running` في Redis. يحتوي الملف أيضًا على نقاط نهاية FastAPI للوحة المعلومات، ولكن لوحظ وجود تكرار لنقاط نهاية سير العمل (`/api/workflow/*`) مباشرة داخل هذا الملف، متجاوزًا `app/workflow_endpoints.py` [^2].

### 4.2. `engine/orchestrator.py`

يعتبر هذا الملف المحرك الرئيسي لعملية اتخاذ القرار. وظيفته الأساسية هي دمج مخرجات جميع وحدات المحرك في `DecisionResult` واحد. إنه المكان الوحيد المسموح له بخلط النقاط والثقة وفحص النظام ومحاذاة الهيكل وتحيز الأطر الزمنية العليا والفحوصات المسبقة للمخاطر. يقوم بمعالجة الشموع المغلقة، ويستدعي وحدات التحليل المختلفة (مثل `structure.py`, `smc.py`, `trend.py`, `risk.py`)، ويجمع إشاراتها، ثم يتخذ قرارًا نهائيًا بشأن الصفقة المحاكاة [^3].

### 4.3. `contracts/decision.py` و `contracts/market.py`

تحدد هذه الملفات نماذج بيانات Pydantic المستخدمة في جميع أنحاء المشروع. `contracts/decision.py` يحدد نماذج مثل `StrategySignal`, `RiskAssessment`, `EntrySignal`, و `DecisionResult`، والتي تمثل مخرجات مسار القرار. `contracts/market.py` (لم يتم قراءته بالكامل ولكن يُفترض من `README.md` و `DESIGN_DOCUMENT.md`) يحدد نماذج بيانات السوق مثل `Candle`, `RegimeState`, `LiquiditySweep`, `OrderBlock`, و `FairValueGap` [^1] [^4]. هذه العقود ضرورية لضمان اتساق البيانات عبر الوحدات المختلفة.

### 4.4. `monitoring/health_manager.py`

ينفذ هذا الملف نظامًا مركزيًا لمراقبة الصحة يحل محل متغير `_health_stats` القديم. يقوم `HealthManager` بتتبع حالة جميع المكونات الفرعية للنظام (مثل Redis, Supabase, Orchestrator)، ويحدث حالتها بناءً على الأحداث المستلمة. كما يقوم بتجميع إحصائيات التشغيل المختلفة (مثل عدد الشموع المستلمة، التحليلات المنفذة، الأخطاء) ويحدد ما إذا كان أي مكون في حالة حرجة (`CRITICAL`) إذا لم يرسل تحديثات الحالة خلال فترة زمنية محددة [^5].

### 4.5. `app/workflow_endpoints.py`

تم تحديد هذا الملف كملف API مستقل لمسارات `/api/workflow`. ومع ذلك، يبدو أنه قديم أو غير متوافق مع العقود الحالية للمشروع. فهو يستورد نماذج بيانات غير موجودة أو غير متسقة مع البنية الحالية، ويحتوي على منطق يفترض هياكل بيانات قديمة. يشير هذا إلى أن `app/main.py` قد تجاوز هذا الملف بمسارات workflow مكررة [^6].

## 5. سير العمل (من الاستيعاب إلى القرار)

يتضمن سير العمل الرئيسي في CT الخطوات التالية [^4]:

1.  **استقبال البيانات**: يتم استقبال الشموع عبر `ingest/binance_ws.py`. يقوم مراقب WebSocket بتحديث حالة الاتصال في `Health Manager`.
2.  **التحليل والقرار**: يقوم `engine/orchestrator.py` بتحليل الشموع واتخاذ القرارات. يقوم مراقب Orchestrator بتحديث حالة حلقة التحليل في `Health Manager`.
3.  **التخزين**: يتم تخزين القرارات والصفقات في قاعدة البيانات عبر `storage/supabase.py`. يقوم مراقب Supabase بتحديث حالة الاتصال في `Health Manager`.
4.  **المراقبة الحية**: يقوم `Health Manager` بتجميع حالة جميع المكونات. يقوم `Heartbeat Generator` بإصدار سجل نبض قلب دوري. يتم تسجيل أي تغيير في حالة المكونات كحدث صحي.
5.  **تجميع النتائج**: يقوم `Result Aggregator` بجمع بيانات القرارات والصفقات من قاعدة البيانات.
6.  **تحليل الأداء**: يقوم `Performance Analyzer` بحساب الإحصائيات والملخصات.
7.  **العرض**: يقوم `Result Formatter` بتنسيق النتائج والإحصائيات. يتم عرض النتائج عبر `bot/telegram_bot.py` أو عبر نقاط نهاية API.

## 6. المراقبة التشغيلية (Observability)

تعتمد المراقبة في CT على نظام تسجيل منظم (structlog) وإدارة صحة مركزية. يتم تسجيل أحداث نبض القلب (`engine_heartbeat`) بشكل دوري لتوفير ملخص لحالة النظام، بما في ذلك وقت التشغيل، عدد الدورات، الشموع المستلمة، التحليلات المنفذة، الأخطاء، والتحذيرات. يتم أيضًا تسجيل الأحداث الصحية (`Health Events`) لتتبع حالة المكونات الفردية وتحديد أي مشكلات [^5] [^7].

## 7. الاختبار

يحتوي المشروع على اختبارات وحدة (`tests/unit/`) واختبارات تكامل (`tests/integration/`). تغطي اختبارات الوحدة كل وحدة من وحدات المحرك والسوق والمحاكاة والمحفظة والتخزين والروبوت. بينما تغطي اختبارات التكامل سيناريوهات تدفق العمل الشاملة، مثل بدء/إيقاف المحرك وتدفقات Telegram [^1].

## 8. الملاحظات والقضايا المحددة

*   **تكرار نقاط نهاية API**: لوحظ أن `app/main.py` يحتوي على تعريفات مكررة لنقاط نهاية `/api/workflow/*`، متجاوزًا الملف المخصص `app/workflow_endpoints.py`. هذا يشير إلى أن `app/workflow_endpoints.py` قد يكون قديمًا أو غير مستخدم [^2] [^6].
*   **عدم اتساق `app/workflow_endpoints.py`**: يبدو أن `app/workflow_endpoints.py` يستخدم نماذج بيانات وعقودًا قديمة أو غير متسقة مع بقية المشروع، مما يؤكد احتمالية كونه ملفًا مهملًا [^6].
*   **نظام مراقبة الصحة الجديد**: تم تنفيذ نظام `HealthManager` مركزي في `monitoring/health_manager.py` ليحل محل آلية `_health_stats` القديمة، مما يوفر رؤية أكثر تفصيلاً ودقة لحالة النظام [^5].

## 9. التوصيات

بناءً على المراجعة، يوصى بالآتي:

1.  **توحيد نقاط نهاية API**: يجب إزالة نقاط نهاية `/api/workflow/*` المكررة من `app/main.py` وتحديث `app/workflow_endpoints.py` ليتوافق مع العقود الحالية للمشروع، أو إزالته بالكامل إذا لم يعد مطلوبًا.
2.  **إزالة الكود القديم**: يجب إزالة `app/workflow_endpoints.py` إذا لم يكن له دور وظيفي حالي، لتجنب الارتباك والحفاظ على نظافة قاعدة الكود.
3.  **الاستفادة الكاملة من `HealthManager`**: التأكد من أن جميع المكونات تستخدم `HealthManager` بشكل صحيح للإبلاغ عن حالتها، وأن لوحة المعلومات تعتمد بشكل كامل على البيانات التي يوفرها `HealthManager`.

## المراجع

[^1]: [CT/README.md](file:///home/ubuntu/CT/README.md) - الوثيقة الرئيسية للمشروع التي تصف الغرض والبنية والهيكل.
[^2]: [CT/app/main.py](file:///home/ubuntu/CT/app/main.py) - نقطة الدخول الرئيسية للتطبيق ومنطق بدء/إيقاف المحرك.
[^3]: [CT/engine/orchestrator.py](file:///home/ubuntu/CT/engine/orchestrator.py) - المحرك الرئيسي لاتخاذ القرار ودمج الإشارات.
[^4]: [CT/DESIGN_DOCUMENT.md](file:///home/ubuntu/CT/DESIGN_DOCUMENT.md) - وثيقة تصميم طبقة المراقبة التشغيلية وحزمة تحليل نتائج الصفقات.
[^5]: [CT/monitoring/health_manager.py](file:///home/ubuntu/CT/monitoring/health_manager.py) - تنفيذ نظام إدارة الصحة المركزي.
[^6]: [CT/app/workflow_endpoints.py](file:///home/ubuntu/CT/app/workflow_endpoints.py) - ملف API لنقاط نهاية سير العمل (تم تحديده كقديم/غير متسق).
[^7]: [CT/monitoring/heartbeat.py](file:///home/ubuntu/CT/monitoring/heartbeat.py) - منطق توليد سجلات نبض القلب.
