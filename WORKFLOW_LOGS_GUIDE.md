# دليل نظام السجلات التشغيلي الاحترافي (Professional Operational Logs Guide)

## نظرة عامة
تم تصميم هذا النظام ليوفر شفافية كاملة ومراقبة دقيقة لكل حركة يقوم بها نظام التداول على منصة Render. السجلات مصممة لتكون "قصصية" تتبع تدفق العمل من لحظة استلام البيانات حتى اتخاذ القرار النهائي وحفظه.

## تنسيق السجل (Log Format)
جميع السجلات تتبع التنسيق الموحد التالي:
`[TIME] [LEVEL] [MODULE] [SYMBOL] [TIMEFRAME] message`

## مستويات السجل (Log Levels)
- **INFO:** للأحداث التشغيلية الرئيسية (بدء دورة، قرار نهائي، حفظ بيانات).
- **DEBUG:** للتفاصيل التقنية الدقيقة (استلام شمعة، نتائج مؤشرات، فحص شروط).
- **WARN:** للتنبيهات التي لا توقف النظام (تأخر بيانات، إعادة اتصال).
- **ERROR:** للأخطاء التي تؤثر على دورة واحدة (فشل تحليل عملة معينة).
- **CRITICAL:** للأخطاء التي توقف النظام بالكامل (فشل الاتصال بقاعدة البيانات أو Redis).

## مراحل دورة العمل في السجلات

### 1. مرحلة الإقلاع والتهيئة (Startup)
عند بدء تشغيل النظام، ستشاهد سجلات توضح:
- إصدار النظام والبيئة.
- حالة الاتصال بـ Redis و Supabase.
- تحميل الإعدادات وعدد العملات المفعلة.

### 2. مرحلة جلب البيانات (Data Ingestion)
- محاولات الاتصال بـ WebSocket Binance.
- استقبال الشموع (الرمز، الفريم، هل هي مغلقة؟).
- تنبيهات في حال وجود تأخير (Latency) في وصول البيانات.

### 3. مرحلة التحليل الفني (Market Analysis)
لكل عملة، يتم تسجيل "خطوات البحث" بالتفصيل:
- **تحليل الاتجاه:** صاعد/هابط وقوة الترند.
- **تحليل البنية (SMC):** كسر الهيكل (BOS)، تغير الاتجاه (CHOCH)، مناطق الطلب والعرض (Order Blocks).
- **تحليل السيولة:** سحب السيولة (Liquidity Sweep) والفجوات السعرية (FVG).

### 4. مرحلة فحص الاستراتيجيات (Strategy Checks)
- فحص كل شرط دخول على حدة.
- ذكر سبب "المرور" (PASS) أو "الرفض" (FAIL).
- بوابة الثقة (Confidence Gate) والدرجة النهائية.

### 5. مرحلة إدارة المخاطر (Risk Management)
- حساب حجم الصفقة المقترح.
- فحص نسبة العائد للمخاطرة (RR Ratio).
- السبب الدقيق لرفض الصفقة إذا لم تتوافق مع معايير المخاطر.

### 6. مرحلة القرار النهائي والتخزين (Final Decision & Storage)
- تسجيل القرار النهائي بوضوح.
- تأكيد حفظ القرار في قاعدة البيانات.
- تأكيد إرسال إشعار تيليجرام.

## ملخص الأداء الدوري
كل 10 دقائق، يقوم النظام بإصدار سجل ملخص (`system_health_summary`) يحتوي على:
- عدد دورات الفحص المكتملة.
- عدد الفرص التي تم العثور عليها.
- أكثر أسباب الرفض تكراراً.
- متوسط زمن دورة التحليل.

## استكشاف الأخطاء (Troubleshooting)
إذا لاحظت توقف الصفقات، ابحث في السجلات عن:
- `regime_check_failed`: السوق متذبذب جداً.
- `confidence_below_threshold`: الاستراتيجيات لا تعطي إشارات قوية كافية.
- `risk_rejected`: حجم الصفقة أو المخاطرة خارج الحدود المسموحة.
- `service_disconnected`: مشكلة في الاتصال بالخدمات الخارجية.
